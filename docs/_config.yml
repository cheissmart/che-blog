# Site Build Settings

title: Che's Blog
email: cheissmart@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  I will add description later
baseurl: "che-blog" # the subpath of your site, e.g. /blog
url: "https://cheissmart.github.io/che-blog" # the base hostname & protocol for your site, e.g. http://example.com
author:  'Che Liu'

# Build settings
# theme: minima
# plugins:
  # - jekyll-feed

# Jekyll-paginate-v2 configuration
#  pagination:
#   enabled: true
#   per_page: 5
#   permalink: '/page/:num/'
#   title: ':title - page :num of :max'
#   limit: 0
#   sort_field: 'date'
#   sort_reverse: true

# jekyll-paginate configuration
paginate:            5

# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           none
plugins:             [jekyll-paginate, jekyll-sitemap, jekyll-feed, jekyll-seo-tag]

# RSS 2.0 can be used instead of Atom by uncommenting following two lines
#feed:
#  path: rss-feed.xml
